# Local Scribe — runtime dependencies
# Pinned to floors known to have wheels for CPython 3.10-3.13.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9

# ASR runtime: CTranslate2 execution of OpenAI Whisper weights.
# Pulls ctranslate2, av (bundled ffmpeg libs), tokenizers, huggingface-hub, onnxruntime.
faster-whisper>=1.1.0,<2.0

# ASR runtime, Apple Silicon GPU: MLX execution of Whisper weights on the
# Mac's own GPU through unified memory. Small, pure-native wheels published
# only for macOS arm64 - the marker makes pip silently skip them everywhere
# else, so unlike requirements-gpu.txt this needs no separate opt-in install.
mlx>=0.18.0 ; sys_platform == "darwin" and platform_machine == "arm64"
mlx-whisper>=0.4.0 ; sys_platform == "darwin" and platform_machine == "arm64"

# Exports
python-docx>=1.1.0,<2.0

# Icon rasterisation (png/ico generation)
pillow>=10.0.0
